#ifndef MINIPAINT_H
# define MINIPAINT_H

# include <libc.h>
# include <math.h>

int	ft_strlen(char *str);
int	pars(char *str);
int	ft_isdigit(char c);
float	ft_atoi(char *str, int *i);
int	ft_error(char *str);

# endif
